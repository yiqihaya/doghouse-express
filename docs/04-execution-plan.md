# 04 — 分步执行计划

## 总览

| 阶段 | 名称 | 预计步骤 | 状态 |
|------|------|----------|------|
| 1 | 项目骨架搭建 | 3 步 | 🔵 进行中 |
| 2 | 数据库与后端 | 3 步 | ⚪ 待开始 |
| 3 | UI 框架 | 3 步 | ⚪ 待开始 |
| 4 | 核心功能 | 4 步 | ⚪ 待开始 |
| 5 | 增强功能 | 3 步 | ⚪ 待开始 |
| 6 | 打包与收尾 | 3 步 | ⚪ 待开始 |

---

## 阶段 1：项目骨架搭建

### 步骤 1.1 — 初始化项目

- [ ] 创建目录结构（docs/, devlog/, renderer/, assets/, data/）
- [ ] 编写 package.json
- [ ] 运行 `npm install` 安装 Electron + better-sqlite3 + electron-builder

**验收**：`npx electron --version` 正常输出版本号

### 步骤 1.2 — 主进程 + 预加载

- [ ] 编写 main.js：创建 BrowserWindow（1200×800）
- [ ] 编写 preload.js：暴露最小 API 骨架
- [ ] 窗口加载 renderer/index.html

**验收**：`npm start` 弹出 Electron 窗口，无报错

### 步骤 1.3 — 前端外壳

- [ ] 编写 renderer/index.html：基础 HTML5 骨架
- [ ] 编写 renderer/style.css：淡蓝背景 + 基础排版
- [ ] 验证窗口显示正确

**验收**：Electron 窗口显示淡蓝色背景，标题为"快递管理"

---

## 阶段 2：数据库与后端

### 步骤 2.1 — 数据库初始化

- [ ] 在 main.js 中引入 better-sqlite3
- [ ] 创建 data/ 目录（如不存在）
- [ ] 建表 SQL（packages 表）
- [ ] 启动时自动初始化

**验收**：启动应用后 data/packages.db 文件自动生成

### 步骤 2.2 — IPC 处理器

- [ ] db:insert — 新增快递记录
- [ ] db:getAll — 获取全部记录（按日期降序）
- [ ] db:search — 模糊搜索
- [ ] db:update — 更新记录
- [ ] db:delete — 删除记录
- [ ] db:getDates — 获取所有日期列表
- [ ] file:pickPhoto — 选择并复制照片到 assets/

**验收**：每个 IPC 通道能正常调用并返回正确数据

### 步骤 2.3 — preload 桥接

- [ ] 在 preload.js 中暴露上述 API
- [ ] 使用 contextBridge.exposeInMainWorld

**验收**：DevTools Console 中 `window.electronAPI` 可访问

---

## 阶段 3：UI 框架

### 步骤 3.1 — 两栏布局

- [ ] 侧边栏（260px）+ 主内容区
- [ ] 侧边栏：标题 + 新增按钮轮廓
- [ ] 主内容区：内容区域占位

**验收**：调整窗口大小，布局自适应

### 步骤 3.2 — 完整样式

- [ ] 按 design-spec.md 色值编写完整 style.css
- [ ] 卡片、按钮、输入框、弹窗样式
- [ ] 字体、圆角、阴影

**验收**：视觉风格与设计规范一致

### 步骤 3.3 — 搜索栏 + 工具栏

- [ ] 顶部搜索栏（含搜索图标）
- [ ] "+ 新增快递" 按钮
- [ ] 工具栏样式

**验收**：工具栏静态效果到位

---

## 阶段 4：核心功能

### 步骤 4.1 — 新增快递

- [ ] 新增表单弹窗（收件人、电话、取件码、快递编号、照片）
- [ ] 照片选择：input[type=file]，筛选图片类型
- [ ] 表单验证（收件人、电话必填）
- [ ] 提交后存入数据库 + 刷新列表

**验收**：录入一条数据后在数据库中确认存在

### 步骤 4.2 — 卡片渲染 + 日期分组

- [ ] 从数据库读取所有数据
- [ ] 按 created_date 分组
- [ ] 每个分组渲染日期标题 + 卡片网格
- [ ] 卡片显示：收件人、取件码、状态角标

**验收**：主区域正确显示分组卡片

### 步骤 4.3 — 详情弹窗 + 复制

- [ ] 点击卡片弹出详情弹窗
- [ ] 显示全部字段 + 照片
- [ ] 每个字段旁复制按钮
- [ ] 复制功能：navigator.clipboard.writeText()

**验收**：点击卡片→查看详情→点击复制→粘贴验证

### 步骤 4.4 — 搜索筛选

- [ ] 搜索输入框实时监听 input 事件
- [ ] 调用 db:search 进行模糊匹配
- [ ] 搜索结果实时更新列表
- [ ] 清空搜索框恢复全部数据

**验收**：输入关键词立即过滤，清空后恢复

---

## 阶段 5：增强功能

### 步骤 5.1 — 编辑功能

- [ ] 详情弹窗中"编辑"按钮
- [ ] 打开预填表单（复用新增表单）
- [ ] 提交后更新数据库 + 刷新
- [ ] 支持更换照片（删除旧照片）

**验收**：修改数据后刷新确认已更新

### 步骤 5.2 — 状态切换

- [ ] 卡片上角标可点击切换
- [ ] "未取" ↔ "已取" 切换
- [ ] 即时更新数据库

**验收**：点击角标切换，刷新后状态保持

### 步骤 5.3 — 侧边栏日期筛选

- [ ] 侧边栏显示月份列表
- [ ] 点击月份筛选该月数据
- [ ] "全部" 按钮恢复全部
- [ ] 当前筛选状态高亮

**验收**：点击不同月份正确过滤数据

---

## 阶段 6：打包与收尾

### 步骤 6.1 — 打包配置

- [ ] electron-builder 配置完善
- [ ] 应用图标（可选）
- [ ] 测试打包脚本

**验收**：`npm run build` 无报错完成

### 步骤 6.2 — 打包测试

- [ ] 生成 exe 文件
- [ ] 在其他目录运行测试
- [ ] 验证数据库和照片路径正确

**验收**：独立 exe 正常运行

### 步骤 6.3 — 最终文档

- [ ] 更新 devlog 总结
- [ ] 生成 README（用户使用说明）
- [ ] 最终检查所有功能

**验收**：文档完整，功能正常
